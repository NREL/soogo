name: Python package

on:
  push:
    branches: ["main"]
    paths:
      - ".github/workflows/python-package.yml"
      - "soogo/**"
      - "tests/**"
      - "pyproject.toml"
  pull_request:
    branches: ["main"]
    paths:
      - ".github/workflows/python-package.yml"
      - "soogo/**"
      - "tests/**"
      - "pyproject.toml"

permissions:
  contents: read

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.9", "3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install uv
          uv venv --python ${{ matrix.python-version }}
          uv pip install -e .
      - name: Lint with ruff
        run: |
          uv pip install flake8 jupyter
          # stop the build if there are Python syntax errors or undefined names
          uv run flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics --exclude .venv,build,__pycache__
          # exit-zero treats all errors as warnings. The GitHub editor is 127 chars wide
          uv run flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics --exclude .venv,__pycache__
      - name: Test with pytest
        if: ${{ matrix.os == 'ubuntu-latest' }}
        run: |
          uv pip install pytest PyNomadBBO pygoblet jupyter ipykernel
          uv run python -m pytest
